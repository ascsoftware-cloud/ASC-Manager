# ASC Manager — for Grok in VS Code

**Paste [`GROK-VSCODE-PROMPT.md`](./GROK-VSCODE-PROMPT.md) into a new Grok chat in VS Code.** That file is the full spec.

That agent should treat it as law. Do not keep Grok App Builder sandbox behaviour (no port-8080 preview contract, no demo logins, no localStorage-as-database).

Short version of the job:

- Keep the UI (emerald ink + champagne, staff top nav, client left rail).
- Kill `Pretoria26` and every seed user.
- Supabase Auth, invite-only, RLS on `tenant_id`.
- Collage / Kloof / Bosveld in the seed are **examples**. Real church logins are created later by invite.
- Vercel for host. Yoco / PayFast only. POPIA.

The rest is in the prompt file.
